machine:
  node:
    version: 5.6.0

dependencies:
  pre:
    - nvm use v5.6.0 && nvm alias default v5.6.0

deployment:
  npm:
    branch: master
    commands:
      - echo -e "$NPM_USERNAME\n$NPM_PASSWORD\n$NPM_EMAIL" | npm login
      - npm run 2npm
